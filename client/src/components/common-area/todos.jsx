import React from 'react';
import TodoListItem from './todo-list-item.jsx';

const Todos = (props) => {
  return (
    <div>
      Todos
      <TodoListItem />
    </div>
  );
};

export default Todos;
